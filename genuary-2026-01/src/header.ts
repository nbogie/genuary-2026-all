export function getAccessibleDescription() {
  return [
    "A single monochrome rectangle shape is scaled uniformly, ",
    "rotated and translated recursively to make a tree. ",
    "Multiple trees are drawn this way, ",
    "each new tree causing the others to blur into the distance somewhat.",
  ].join("\n");
}
export function aboutThisSourceCode() {
  return [
    "This javascript is probably unreadable",
    "It has been transpiled and bundled from typescript source",
    "With no control of module or function order",
  ].join("\n");
}
